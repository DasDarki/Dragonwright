services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: dragonwright_user
      POSTGRES_PASSWORD: your_password_here
      POSTGRES_DB: dragonwright_db
    ports:
      - "25432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: